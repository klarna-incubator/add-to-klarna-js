# Examples

- `usage.ts` — the smallest "real" snippet showing how a merchant would call
  the library from TypeScript. Compile-only.
- `usage.mjs` — the same, in plain JavaScript, against the built `dist/` files
  (run `yarn build` first).
